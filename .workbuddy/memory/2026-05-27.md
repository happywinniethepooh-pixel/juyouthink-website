# 2026-05-27 工作日志

## 修复中英双语切换bug
- **根因**：`shared.css` 中 `.lang-zh { display: none; }` 与 `lang.js` 中 `el.style.display = ''` 冲突
- CSS规则会在内联样式被清除后重新接管，导致中文永远被隐藏
- **修复**：`lang.js` 中将 `''` 改为 `'inline'`，内联样式覆盖CSS规则
- `index.html` 内联的 lang.js 替换为引用 `<script src="lang.js"></script>`

## 从PPTX提取图片
- 用户提供 `D:/工作/公司介绍/公司介绍20260514无美国.pptx`
- 用Python zipfile提取52个媒体文件至 `images/pptx-extracted/`
- 识别关键图片：城市天际线(hero)、团队照(Shinny/Kevin)、客户logo(美的/雀巢/欧莱雅/海尔/华润)、场景图
- 复制16个关键图片至 `images/` 并用描述性文件名

## services.html 全面双语化
- 6个服务卡片全部添加中英双语（消费者研究、产品创新、渠道、品牌、大数据、一站式方案）
- 4个方法步骤添加双语（共创简报、定制设计、资深主导执行、可落地成果）
- 3个方法卡片添加双语（入户深访、小组座谈会、在线定量调研）
- 大数据卡片添加数据可视化图片
- Hero区域添加城市背景图

## team.html 更新
- Shinny和Kevin替换为实际照片（image9.png, image10.png）
- Polly保留首字母头像（缺照片）
- 三个成员全部添加中文简介（基于PPTX内容）
- 所有客户标签添加中英双语
- Hero区域添加背景图

## 其他页面图片补充
- index.html：Hero添加城市背景图，使用lang.js替代内联JS
- cases.html：波轮洗衣机案例添加居家图片，茶饮案例添加饮料图片
- why-us.html：Hero + 覆盖范围区域添加图片
- contact.html：添加研究场景装饰图
- 所有子页面Hero区域统一添加城市天际线背景图

## why-us.html 中文措辞优化
- 卡片04（顶级背景精品专注）：删除英文标题和Kantar/凯度/机构级方法论引用，全用中文，改为"创始团队拥有世界500强企业研究背景，15年以上中国市场调研经验。我们既能提供大公司级的研究严谨度，又保持精品公司特有的灵活性和定制化服务。"
- AI对比表中文调整：
  - "无可落地的建议" → "难以落地的策略建议"
  - "生成模式而非处方" → "输出模式化"
  - "无法将发现转化为商业决策"从第4行移至第5行"通用输出"末尾

## 主色调改为蓝+红、Logo替换、中文公司名
- 用户要求网页主色调匹配PPT文件的蓝色和红色
- **颜色变更**：`--gold: #D4A843` → `--red: #C41230`，`--gold-light: #F0D78C` → `--red-light: #E8556D`
- 红色 `#C41230` 取自公司logo印章红，与 `--blue-600: #2471A3` 形成蓝红双主色体系
- 所有文件中的 `.btn-gold` 改为 `.btn-red`（白色文字，红底按钮）
- 所有 `var(--gold)` / `var(--gold-light)` CSS引用同步改为 `var(--red)` / `var(--red-light)`
- **Logo替换**：`D:/工作/公司介绍/聚优 logo.png` 复制到 `images/juyou-logo.png`（9.7KB）
- 全部6个HTML的导航栏和页脚logo从 `<div class="nav-logo-icon">JY</div>` 改为 `<div class="nav-logo-icon"><img src="images/juyou-logo.png" /></div>`
- **中文公司名**：`nav-logo-text` 改为双语 `<span class="lang-en">JuYou Think</span><span class="lang-zh" style="display:none">聚优</span>`
- `shared.css` 的 `.nav-logo-icon` 从固定尺寸文字样式改为支持 `<img>` 的容器样式
- `index.html` 内联CSS中所有gold引用同步改为red（nav hover、hero eyebrow、hero stat、case industry、footer links等）
- 子页面内联CSS残留gold也修复（services方法步骤线、cases案例行业标签、why-us对比表VS圆形和标题）

## 颜色体系精调 — 对齐PPT色板
- 用户反馈蓝红不够协调，要求参照PPT配色
- **分析PPT实际颜色**：从PPTX的17页幻灯片中提取22个唯一色值
  - PPT主蓝：`#198ACD`（亮天蓝，比当前 `#2471A3` 更明亮纯净）
  - PPT主红：`#C00000`（纯正中国红，比当前 `#C41230` 更少粉调）
  - **Logo红色也是 `#C00000`** — PPT与Logo完全一致
- **整条蓝色色阶重新计算**（基于 `#198ACD` 的HSL 203,78,45）：
  - `--blue-900: #091F2F`（旧 `#0F2B46`）— 更深更纯的navy
  - `--blue-600: #198ACD`（旧 `#2471A3`）— 主品牌蓝
  - 整条从900到50全部按HSL梯度重新推导
- **红色调整**：
  - `--red: #C00000`（旧 `#C41230`）— 对齐logo印章红
  - `--red-light: #D33`（旧 `#E8556D`）— 更纯的红而非粉
- **所有6个页面的hero硬编码渐变同步更新**：

## 蓝色logo替换 + 字体优化 + 红色降级为点缀
- 用户要求：(1)logo换为蓝色版 (2)蓝色为主基调，红色仅作点缀 (3)去除logo白边 (4)中文微软雅黑为主，弃用宋体感死板风格
- **Logo去白底处理**：PIL逐像素检测白色/浅灰，设透明。保存为 `images/juyou-logo-blue.png`，不透明像素49.7%
- **Logo全面替换**：全部6页面 `juyou-logo.png` → `juyou-logo-blue.png`
- **字体**：弃用Playfair Display；`--serif`→`--heading: Inter/微软雅黑`；h1-h4统一heading字族，h1加-0.02em letter-spacing；nav-logo-text也改为heading
- **红色降级（仅保留btn-red按钮+why-us VS圆圈）**：nav hover、hero eyebrow/span/stat、case industry、footer links、page-hero label、services步骤线、why-us adv-col-header全部从red改为blue
  - 旧 `rgba(15,43,70,..)` → 新 `rgba(9,31,47,..)`
  - 旧 `rgba(26,82,118,..)` → 新 `rgba(20,74,108,..)`
  - 旧 `rgba(36,113,163,..)` → 新 `rgba(25,138,205,..)`
- 修改文件：`shared.css`, `index.html`（含内联:root和hero渐变），`services.html`, `cases.html`, `why-us.html`, `contact.html`, `team.html`

## team.html 删除 Shinny + 头像迁至 Polly
- 删除 Shinny Liang (梁皓倩) 的整个 team-card
- Shinny 的头像图片 `images/pptx-extracted/image9.png` 迁移到 Polly Sun 的 team-avatar 中，替代 "PS" 文字首字母
- 团队网格从 `repeat(3, 1fr)` 改为 `repeat(2, 1fr)`，适配2人布局

## contact.html 接入 Formspree
- 表单从纯占位（假提交）改为真正的 Formspree 集成
- 添加 `action="https://formspree.io/f/YOUR_FORM_ID"` + `method="POST"`
- 所有输入框添加 `name` 属性（name, company, email, service, message）
- JavaScript 改用 fetch() 异步提交，支持成功/失败/网络错误三种状态反馈
- 按钮状态：Sending... → Message Sent! ✓（绿）/ Error（红）/ Network Error（红）
- **待用户提供 Formspree form ID 后替换 YOUR_FORM_ID**

## why-us.html 删除"决策就绪的建议"行
- 从 JuYou Think vs AI-Only 对比表中删除 "Decision-Ready Recommendations / 决策就绪的建议" 整行
- 现在聚优侧对比项从5项减为4项：入户深访、本土文化智慧、实时田野发现、定制方法论

## 主页视觉美化（UI Designer 设计精修）

### Hero 区域
- 背景圆形元素浮动动画（floatCircle1/2，14s/18s周期）
- 新增第三个背景圆形（circle3，反向浮动）
- 背景线条透明度脉冲动画
- h1 span 文字光晕效果（text-shadow）
- 数据统计改为毛玻璃卡片样式（半透明背景+模糊+边框+悬浮）
- 统计卡片逐条渐入动画（0.85s-1.15s stagger）
- Hero所有文字元素递进加载动画
- 新增滚动提示按钮（底部圆形脉冲动画箭头）

### 客户 Logo 条
- 渐变顶部装饰线（蓝渐变）
- 统一上下边框，Logo悬浮缩放效果

### Why Us 卡片
- 每张卡片顶部3px渐变色彩条（四色四种渐变）
- 图标升级为52×52渐变圆角方块
- 编号放大至4rem，悬浮变淡蓝
- 悬浮上浮6px+阴影加深
- 区域背景两端白中间蓝渐变
- 卡片间gap: 1.5rem，独立圆角+阴影
- 滚动渐入错开延迟动画

### CTA 横幅
- 顶部椭圆波浪分割线（clip-path）
- 双层径向渐变+点阵背景
- 按钮加大圆角+红色悬浮阴影
- padding从80→100px

### 全局
- 响应式适配新布局（hero-stat移动端50%宽）

## 客户Logo去白底 + 雀巢/长虹放大 + Why Us标点修正
- **所有9个客户Logo去白底**：PIL逐像素检测白色(R>235,G>235,B>235)并设透明，保存为 `images/logo_*.png`
  - 涉及：L'Oreal, Haier, Midea, Vivo, Xiaomi, Nestlé, Changhong, KUKA Home, China Resources
- **雀巢和长虹放大2.5倍**：从30px高度扩至75px（logo_nestle.png 98×75, logo_changhong.png 75×75）
- HTML中 `<img>` 引用从原文件（imageXX.jpeg/png）改为处理后的 `logo_*.png`
- 雀巢和长虹的 `<img>` 添加 `style="height:75px;max-width:none"` 内联样式
- **Why Us** 中文标题"精品深度。全球标准。"删除句号 → "精品深度 全球标准"
